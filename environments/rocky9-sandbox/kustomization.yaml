apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# rocky9-sandbox overlay — VALUES ONLY. Structure lives in components/.
resources:
  - ../../components/_base
components: []
# Component references are added here as they are implemented (Stages 1-7).
labels:
  - pairs:
      talu.io/environment: rocky9-sandbox
    includeSelectors: false
