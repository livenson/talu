# examples — consuming the tenant API

Example tenant values files and VM orders that render `components/tenancy/tenant-chart`.
These demonstrate the §10 external-manager contract (write labelled objects, watch
status, read Prometheus, delegate OIDC identity) with no manager present.
