apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Skeleton — real manifests (HelmRelease / CRs) land here per the pilot plan.
# Intentionally empty so `kustomize build` succeeds during scaffolding.
resources: []
