apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# tuppr TalosUpgrade/KubernetesUpgrade CRs + CEL health gates land here per the pilot plan.
# First real resource: the compat-matrix guardrail (the machine-readable version-compat source
# the Kyverno gate / Renovate / CI all read).
resources:
  - compat-matrix.yaml
