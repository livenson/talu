apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# OFF BY DEFAULT — deliberately not referenced by any environments/<site>/kustomization.yaml.
# Enabling this changes a cluster-wide failure domain; read ./README.md first.
resources:
  - deployment.yaml
  - alerts.yaml
