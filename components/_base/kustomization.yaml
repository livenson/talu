apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Platform namespaces + common identity applied to every Talu-managed object.
resources:
  - namespaces.yaml
labels:
  - pairs:
      app.kubernetes.io/part-of: talu
    includeSelectors: false
