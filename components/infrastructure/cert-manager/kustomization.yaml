apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The cert-manager operator is installed from its release manifest (README); this overlay
# adds the internal-CA issuer chain. Validated on the lab: talu-ca ClusterIssuer issues leaf certs.
resources:
  - issuers.yaml
