apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The golden-image catalog (namespace + clone RBAC + DataImportCrons/DataSources). The CDI operator
# itself is installed by the kubevirt ansible role; environments override the DataImportCron sources.
resources:
  - catalog.yaml
